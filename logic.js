// import { getColoredLine } from "./data.js";
// const coloredLines = getColoredLine();
const hasSquare = (array) => {
  for (let i = 0; i < array.length; i++) {
    for (let j = 0; j < array.length; j++) {
      if (
        array[i].i === array[j].i &&
        Math.abs(array[i].j - array[i].j) === 2
      ) {
        

      }
    }
  }
};
