import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
  root: {
    backgroundColor: 'rgb(0, 170, 30)',
    padding: '5px 40px',
    borderRadius: '5px',
    color: 'white',
  },
}))
