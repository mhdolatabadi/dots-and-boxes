// import clsx from 'clsx'

// export const ab = className => test => Boolean(test) && className
// export const cns = clsx
/**
 * use following instead
 * import cns form 'clsx'
 *
 * cns({ [classes.root]: isRoot }, props.className)
 *
 */
